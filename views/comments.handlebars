<div class="comments">
    {{#each this}}
    <section class="comment">
        <div class="meta">
            {{user.name}} on {{creation_data}}
        </div>
        <div class="text">
            {{comment_details}}
        </div>
    </section>
    {{/each}}
</div>